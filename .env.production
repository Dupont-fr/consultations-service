# Consultation Service - Production (Render)
# Ces variables sont définies dans le dashboard Render
# Ne pas committer ce fichier

NODE_ENV=production
PORT=3003
HOST=0.0.0.0

# Remplacer par l'URL PostgreSQL fournie par Render
DATABASE_URL=postgresql://user:password@host.render.com:5432/consultations_db

# JWT Secret (identique pour tous les services)
JWT_SECRET=your-production-jwt-secret

# URL du Gateway en production
CORS_ORIGIN=https://votre-gateway.onrender.com
